object CheckCommand "mysqldump" {
  import "plugin-check-command"

  command = [ "/usr/bin/sudo", "/usr/local/lib/icinga2-plugins/check_mysqldump-secure", "-i26 -f/backup/$host.name$/rdiff-backup/var/backups/mysqldump-secure.monitoring.log" ]
}
